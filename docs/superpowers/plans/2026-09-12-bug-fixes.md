# Verified Bug Fixes Implementation Plan

> Execute one task at a time in this session, with RED/GREEN evidence. User authorized implementation from the 2026-09-12 review. Preserve pre-existing untracked files.

**Goal:** Repair the ten findings in `docs/validation/2026-09-12-bug-review.md`.
**Architecture:** Keep existing renderers and nonce/CSP. Share a text-node math pass, apply plain-text highlighting to source offsets through the already bundled bounded engine, and propagate saved configuration to the host WebView. Image policy uses default-off with explicit Settings opt-in; this is the stated implementation assumption after the optional preference question received no reply.
**Tech Stack:** Swift 6, macOS 15+, JavaScriptCore, WKWebView, SwiftUI, XCTest.
**Spec:** `docs/superpowers/specs/2026-03-30-quicklook-extension-design.md`, current Security Note and the authorized review's correction requirements.

## Constraints

- No nonce on document-supplied scripts; no external JS/CSS tags or relaxed navigation.
- Preserve raw HTML in Markdown and notebook output.
- Runtime assertions, host build, visual and Finder Space checks are separate evidence.
- Existing bounded matching semantics and hard caps remain. This task integrates plain-text source lines only; future Markdown DOM pattern highlighting is not implemented.

## Task 1: Script boundaries

Files: `ScriptEscaping.swift`, `Tests/RendererRegressionTests.swift`.
- [x] RED: load Markdown `# Visible\n\n<!--<script>` and highlighted text `before <!--<script> after`; assert visible heading/text, document readiness and no foreign script execution.
- [x] GREEN: neutralize comment opening after JS literal escaping using `.replacingOccurrences(of: "<!--", with: "<\\x21--")`; retain script-end protection.
- [x] Run RendererRegressionTests and ScriptEscapingTests.

## Task 2: Markdown and notebook source preservation

Files: `MarkdownRenderer.swift`, `NotebookRenderer.swift`, `HTMLTemplate.swift`, new `Shared/Resources/js/render-math.js`, regression tests.
- [x] RED: fenced/inline code with `$HOME$` stays literal in Markdown and notebook; notebook inline `a < b & c` stays exact and `<b>bold</b>` creates a bold element; math with `<`, `&` and escaped dollar remains correct.
- [x] GREEN: parse notebook raw source from `textContent`; load shared app-controlled math helper inline. Walk text nodes, reject `pre,code,script,style,textarea,svg,math,.katex`, render paired delimiters once into a fragment, never rewrite attributes or generated math. Apply discovery/text/expression limits with a visible skipped notice.
- [x] Run regression, notebook, Markdown and live WebView tests.

## Task 3: Plain-text rendering

Files: `PlainTextRenderer.swift`, new `LogPatternHighlighter.swift`, regression tests.
- [x] RED: configured font applies to code, line numbers on/off preserve content; `&` log pattern preserves `a < b & c`; overlapping level patterns preserve text and deterministic error precedence; oversized/adversarial inputs degrade with a notice rather than unbounded work.
- [x] GREEN: separate line-number gutter from code content for syntax rendering; inherit code font. Compile four level patterns through JavaScriptCore's bundled bounded compiler once per document, share matcher budgets, resolve source intervals and escape slices only when emitting HTML. Unsupported/exhausted patterns leave text readable with a notice. Bound plain-text preview allocation and line count with explicit truncation notices.
- [x] Run regression plus bounded engine and plain-text tests; rerun 96/192/384 KB timing probe.

## Task 4: Saved settings and WebView lifetime

Files: `ConfigLoader.swift`, host Views, shared preview document/state as needed, tests.
- [x] RED: successful config save notifies and failed save does not; re-render same sample after settings change uses new style; WebView timeout change reaches the native image loader.
- [x] GREEN: publish successful saves, reload selected sample on save, carry timeout as rendered configuration and recreate WebView when transport options change.
- [x] Run config and integration tests, host build and Settings/Preview live UI check.

## Task 5: Image policy

Files: `ConfigSchema.swift`, `default-config.json`, `SettingsView.swift`, `PreviewWebView.swift`, `ExternalImageSchemeHandler.swift`, renderers/template, tests and design amendment.
- [x] Record policy assumption: default-off with Settings opt-in; preference question unanswered, no claim of explicit owner selection.
- [x] RED: blocked mode starts no requests even for document-authored `quicklook-image:` URLs; data images still display; opt-in mode retains image timeout/size limits; old JSON decodes to the secure default.
- [x] GREEN: enforce selected policy at native transport boundary and show an explanatory placeholder; propagate option through host and extension.
- [x] Run configuration, image loader, CSP and WebView tests.

## Task 6: Final verification

Evidence and scope: `docs/validation/2026-09-12-bug-fixes.md`. Independent review added inside/outside-math escaped-dollar and extension reuse regressions; Finder testing additionally required a stable root NSView with replaceable WebView child.

- [x] `xcodegen generate`, complete Tests scheme, host/extension build, diff check.
- [x] Verify current build in live host and Finder Space for md/ipynb/log; verify running extension path and restore temporary registration changes.
- [x] Review diff and record actual assertions, performance measurements, visual checks and any unrun checks in a new validation report; update HANDOFF to the actual checkpoint.

Commands: `xcodebuild test -project AllYouNeedQuickLook.xcodeproj -scheme Tests -destination 'platform=macOS' -derivedDataPath /private/tmp/aynql-fixes-20260912` (add `-only-testing:Tests/<class>` for each RED/GREEN); host build uses same project/path with `-scheme AllYouNeedQuickLook build`.
