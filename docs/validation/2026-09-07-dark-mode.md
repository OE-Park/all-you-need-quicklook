# 다크 모드 검증과 호스트 Preview 복구 — 2026-09-07

## 대상과 판정

기준 main `99c185015331881fb2230153b817f14c8a968fe4`, 수정 브랜치
`fix/host-preview-webkit`, 워크트리 `.worktrees/pr-merge`.
macOS 26.6.2 / Xcode 26.6. 최초 다크 검증에서 발견한 호스트 Preview 빈 화면을
호스트의 network.client entitlement 추가로 해결했다. 수정 후 라이트와 다크에서
샘플 4종(md/txt/log/ipynb)의 실제 렌더링을 확인했다.

## 원인과 변경

기준 빌드에서 호스트 Preview 샘플 4종이 비었고 WebKit 자식 프로세스의
`processDidTerminateOrFailedToLaunch: reason=Crash`와 sandbox extension 생성 실패가
반복되었다. 같은 빌드의 XCTest WKWebView와 Finder 확장은 성공했다.
호스트에만 `com.apple.security.network.client`가 없었다.

`project.yml`의 호스트에 해당 권한을 추가하고 XcodeGen으로 entitlements를 재생성했다.
서명된 앱에도 app-sandbox, application-groups, network.client가 적용됨을 codesign으로
확인했다. 이 한 가지 런타임 설정 변경 후 실제 호스트 미리보기가 복구되었다.
샌드박스의 로컬 HTML WebKit 실행에도 필요한 권한이라는
[동일 증상 보고](https://developer.apple.com/forums/thread/116359)와 부합한다.
이는 외부 연결 권한을 호스트에 추가하는 변경이다. CSP, nonce, 탐색 제한,
렌더러 소스 및 확장 권한은 변경하지 않았다.

## 실행 검증

| 범위 | 결과 |
|---|---|
| 수정 전 다크 XCTest | 기존 171 + 세션 probe 4 = 175 통과; 샌드박스 호스트 성공을 증명하지 못함 |
| 수정 후 기존 XCTest | 171 통과, 실패 0 |
| 수정 후 다크 XCTest | 기존 171 + 세션 probe 4 = 175 통과, 실패 0 |
| 수정 후 호스트 빌드 | 성공, ad-hoc 서명 및 실제 signed entitlements 확인 |
| 호스트 라이트 실제 UI | md 제목/코드/표/수식, txt 공백/특수문자, log 줄번호/색상, ipynb 코드/출력/이미지 확인 |
| 호스트 다크 실제 UI | 같은 4종 표시, 어두운 배경과 밝은 글자 확인. 실행 중 라이트→다크 전환도 반영 |
| Finder Space | 수정 전 같은 기준 커밋의 md/ipynb/log 다크 미리보기 통과. 이번 호스트 권한 수정 후 재실행하지 않음 |

다크 probe는 실제 WKWebView에서 matchMedia dark=true, 비어 있지 않은 본문,
body 배경 rgb(29,29,31), 글자 rgb(245,245,247), md/ipynb 코드 배경
rgb(13,17,23), log ERROR rgb(255,107,107)를 assertion으로 확인한다.
시스템 테마에 의존하므로 CI 테스트에 넣지 않고 아래 재현 자료로 보존한다.
노트북 래스터 출력 이미지의 흰 배경은 이미지 자체 내용이다.

수정 전 Finder 검증에서는 실행 PID 94412의 경로가
`/private/tmp/quicklook-pr-validation/Build/Products/Debug/AllYouNeedQuickLook.app/Contents/PlugIns/QuickLookExtension.appex/Contents/MacOS/QuickLookExtension`
임을 확인했다. 이번 변경은 호스트 entitlement에만 영향을 주며 확장/렌더러는 동일하다.

## 재현 명령과 증거

```sh
xcodegen generate
xcodebuild test -project AllYouNeedQuickLook.xcodeproj -scheme Tests -destination 'platform=macOS' -derivedDataPath /private/tmp/quicklook-pr-validation
xcodebuild -project AllYouNeedQuickLook.xcodeproj -scheme AllYouNeedQuickLook -destination 'platform=macOS' -derivedDataPath /private/tmp/quicklook-pr-validation build
codesign -d --entitlements - /private/tmp/quicklook-pr-validation/Build/Products/Debug/AllYouNeedQuickLook.app
```

다크 probe 재실행: 시스템 다크 전환 → 아래 Swift 파일을 `Tests/`에 임시 복사 →
xcodegen generate → test → 임시 Tests 파일 제거 → xcodegen generate.
호스트 실제 회귀 검증: 빌드한 앱 실행 → Preview → 4개 샘플 선택 → 본문 시각 확인 →
라이트/다크 전환 → 본문 재확인. XCTest만으로 이 회귀를 통과 처리하지 않는다.

- [재현용 다크 probe](artifacts/2026-09-07/DarkModeValidationTests.swift)
- [최초 다크 테스트](artifacts/2026-09-07/dark-tests.log), [최초 빌드](artifacts/2026-09-07/dark-build.log)
- [실패 로그 발췌](artifacts/2026-09-07/host-failure-excerpt.log)
- [수정 후 기존 테스트](artifacts/2026-09-07/host-fix-tests.log), [수정 후 빌드](artifacts/2026-09-07/host-fix-build.log)
- [수정 후 다크 테스트](artifacts/2026-09-07/host-fix-dark-tests.log)

위 로그는 `scripts/sanitize-validation-log.sh`로 계정 경로·소유자 환경변수·`export PATH`·
destination 하드웨어 id를 자리표시자로 바꾼 상태다. 명령, 타임스탬프, assertion,
빌드/테스트 결과는 원본 그대로이므로 근거로서의 범위는 달라지지 않는다.

## 정리와 범위

검증 앱 종료 및 임시 등록 제거. PlugInKit에서 기존 5개 등록 유지 확인.
사용자가 전환한 시스템 다크 모드는 유지했다. 설치된 `/Applications` 앱을 교체하지 않았다.
이번 결과는 보고서의 빈 화면과 샘플 테마 검증에 한정한다. 아직 구현하지 않은
render-time 기능이나 외부 이미지 네트워크 동작의 신규 검증을 뜻하지 않는다.

## 후속 설치 — 2026-09-07

사용자의 앱 교체·커밋·PR 요청에 따라 `98eb5d6`의 검증 빌드를
`/Applications/AllYouNeedQuickLook.app`에 설치했다. 위 절의 미교체 상태는 설치 전 기록이다.

- 기존 앱 백업: `~/Library/Application Support/AllYouNeedQuickLook/Backups/AllYouNeedQuickLook-before-98eb5d6.zip` (ZIP 무결성 확인).
- 설치본 deep/strict 서명 검증 성공. signed entitlement에 sandbox/app-group/network.client 확인.
- 설치본과 검증 빌드의 호스트 실행 파일 SHA-256 동일:
  `1c6c67b5dc6dfd2e35b62f8996598edba7015f1e1501eb66503320a21db86ade`.
- 설치 앱 Preview의 sample.md 다크 본문·코드·표·수식 실제 표시 확인.
- 설치 후 Finder Space sample.md 다크 미리보기 확인. PID 11908의 실행 경로가
  `/Applications/AllYouNeedQuickLook.app/Contents/PlugIns/QuickLookExtension.appex/Contents/MacOS/QuickLookExtension`임을 확인.
- 호스트 PID 11372도 `/Applications/AllYouNeedQuickLook.app/Contents/MacOS/AllYouNeedQuickLook`.
- 기존 개발 빌드 등록 4개 유지, 설치 확장 등록 갱신 (총 5개). Finder 검증 패널 닫음.
- 설치 후 smoke check는 Markdown에 한정. 샘플 4종·175개 테스트 결과는 앞 절의 동일 검증 빌드 결과다.
